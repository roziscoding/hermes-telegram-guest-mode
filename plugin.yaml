name: telegram-guest
label: Telegram Guest Mode
kind: standalone
version: 0.2.1
description: >
  Adds native Telegram Guest Mode support by subclassing the bundled
  Telegram adapter. Normal Telegram traffic always uses the upstream paths.
author: Aster + Roz
